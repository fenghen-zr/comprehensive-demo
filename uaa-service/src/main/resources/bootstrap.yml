spring:
  application:
    name: uaa-service
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
  #      discovery:
  #        enabled: true
  #        service-id: config-server
  profiles:
    active: pro