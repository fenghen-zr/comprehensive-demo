spring:
  application:
    name: gateway-service
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8769
#      #从git上读取配置，需要此配置
#      discovery:
#        enabled: true
#        service-id: config-server

  profiles:
    active: pro